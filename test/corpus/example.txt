===============================
Expansion
===============================
%%%Z is %{unseen-count}

---
(pattern
  (variable
    (simple_expansion))
  (variable
    (simple_expansion))
  (variable
    (expansion)))

===============================
Nested If
===============================
%<S?%<N?new>&%?n?old?>

---
(pattern
  (variable
    (if
      (start_if)
      (condition)
      (pattern
        (variable
          (if
            (start_if)
            (condition)
            (pattern)
            (end_if))))
      (pattern
        (variable
          (if
            (start_if)
            (condition)
            (pattern)
            (end_if))))
      (end_if))))

===============================
Padding
===============================
%>-$var

---
(pattern
  (variable
    (padding
      (padding_type)
      (padding_character)))
  (user_variable
    (identifier)))
